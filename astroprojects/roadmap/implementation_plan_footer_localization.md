# フッター多言語対応実装計画

## ユーザーレビューが必要な項目
- 特になし

## 変更案

### [src/data/ui.ts] (New)
- UI用の翻訳データを定義します。
- `footer` オブジェクトを作成し、各言語のテキストを定義します。

### [src/components/Footer.astro]
- `src/data/ui.ts` をインポートします。
- `lang` パラメータに基づいてテキストを切り替えます。

## 検証計画
- `npm run dev` で各言語ページ (`/jp/`, `/en/`, `/zh/`) にアクセスし、フッターが切り替わっているか確認します。
