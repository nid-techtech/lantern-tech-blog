---
import { ui } from "../data/ui";
import {
    categoryNames,
    blogPages,
    newsreleasePages,
    notesPages,
} from "../data/navigation";

const { lang } = Astro.params;

// langがundefinedまたはサポート外の場合のフォールバック
const currentLang =
    lang && typeof lang === "string" && (lang === "jp" || lang === "zh")
        ? lang
        : "en";
const t = ui[currentLang].footer;
const cats = {
    top: categoryNames.top[currentLang],
    blog: categoryNames.blog[currentLang],
    newsrelease: categoryNames.newsrelease[currentLang],
    notes: categoryNames.notes[currentLang],
};

const externalLinks = {
    jp: {
        astro: {
            href: "https://astro.build",
            label: "Astro | 世界最速のWebフレームワーク",
        },
        cloudflare: {
            href: "https://pages.cloudflare.com",
            label: "Cloudflare Pages | 記録的な速さのサイトを構築しよう",
        },
        imgur: {
            href: "https://imgur.com",
            label: "Imgur | インターネットの魔法",
        },
        z4a: {
            href: "https://img.z4a.net",
            label: "Z4A図床 | 中国で最も誠実な画像ホスティング",
        },
    },
    en: {
        astro: {
            href: "https://astro.build",
            label: "Astro | World's Fastest Web Framework",
        },
        cloudflare: {
            href: "https://pages.cloudflare.com",
            label: "Cloudflare Pages | Build fast sites. In record time.",
        },
        imgur: {
            href: "https://imgur.com",
            label: "Imgur | The magic of the Internet",
        },
        z4a: {
            href: "https://img.z4a.net",
            label: "Z4A Picture Bed | The most honest image hosting in China",
        },
    },
    zh: {
        astro: {
            href: "https://astro.build",
            label: "Astro | 世界最快的Web框架",
        },
        cloudflare: {
            href: "https://pages.cloudflare.com",
            label: "Cloudflare Pages | 快速构建网站。创纪录的速度。",
        },
        imgur: {
            href: "https://imgur.com",
            label: "Imgur | 互联网的魔法",
        },
        z4a: {
            href: "https://img.z4a.net",
            label: "Z4A图床 | 做中国最良心的免费图床",
        },
    },
};

const getFaviconUrl = (url: string) => {
    const domain = new URL(url).hostname;
    return `https://www.google.com/s2/favicons?domain=${domain}`;
};
---

<footer>
    <div class="footer-content">
        <div class="footer-section">
            <h3>{t.sitemap}</h3>
            <ul>
                <li><a href={`/${lang}/`}>{cats.top}</a></li>
                <li>
                    <a href={`/${lang}/blog`}>{cats.blog}</a>
                    <ul class="sub-links">
                        {
                            blogPages.map((page) => (
                                <li>
                                    <a href={`/${lang}/blog/${page.slug}`}>
                                        {page.title[currentLang]}
                                    </a>
                                </li>
                            ))
                        }
                    </ul>
                </li>
                <li>
                    <a href={`/${lang}/newsrelease`}>{cats.newsrelease}</a>
                    <ul class="sub-links">
                        {
                            newsreleasePages.map((page) => (
                                <li>
                                    <a
                                        href={`/${lang}/newsrelease/${page.slug}`}
                                    >
                                        {page.title[currentLang]}
                                    </a>
                                </li>
                            ))
                        }
                    </ul>
                </li>
                <li>
                    <a href={`/${lang}/notes`}>{cats.notes}</a>
                    <ul class="sub-links">
                        {
                            notesPages.map((page) => (
                                <li>
                                    <a href={`/${lang}/notes/${page.slug}`}>
                                        {page.title[currentLang]}
                                    </a>
                                </li>
                            ))
                        }
                    </ul>
                </li>
            </ul>
        </div>

        <div class="footer-section">
            <h3>{t.links}</h3>
            <ul>
                {
                    Object.values(externalLinks[currentLang]).map(
                        (link: any) => (
                            <li>
                                <a
                                    href={link.href}
                                    target="_blank"
                                    rel="noopener noreferrer"
                                    class="external-link"
                                >
                                    <img
                                        src={getFaviconUrl(link.href)}
                                        alt=""
                                        width="16"
                                        height="16"
                                        class="favicon"
                                    />
                                    {link.label}
                                </a>
                            </li>
                        ),
                    )
                }
                <!-- Add more external links here -->
            </ul>
            <div class="footer-description">
                <h3>Lantern-TECH Blog</h3>
                <p>{t.description}</p>
            </div>
        </div>
    </div>

    <div class="copyright">
        <span set:html={t.poweredby} /> &copy; Lantern-TECH 2025 {t.rights}
    </div>
</footer>

<style>
    footer {
        background-color: var(--color-primary);
        color: #ffffff;
        padding: 3rem 1rem 1rem;
        margin-top: auto;
    }

    .footer-content {
        max-width: 1200px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
        margin-bottom: 2rem;
    }

    @media (max-width: 768px) {
        .footer-content {
            grid-template-columns: 1fr;
        }
    }

    .footer-section h3 {
        color: var(--color-accent);
        margin-top: 0;
        margin-bottom: 1rem;
    }

    .footer-section ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .footer-section .sub-links {
        padding-left: 20px;
        margin-top: 0.25rem;
        font-size: 0.9em;
        opacity: 0.9;
    }

    .footer-section li {
        margin-bottom: 0.5rem;
    }

    .footer-section a {
        color: #ffffff;
        opacity: 0.8;
    }

    .footer-section a:hover {
        opacity: 1;
        color: var(--color-accent);
    }

    .favicon {
        width: 16px;
        height: 16px;
        margin-right: 0.5rem;
        vertical-align: text-bottom;
    }

    .external-link {
        display: flex;
        align-items: center;
    }

    .footer-description {
        margin-top: 2rem;
        padding-top: 2rem;
        /* border-top: 1px solid rgba(255, 255, 255, 0.1); */
    }

    .copyright {
        text-align: center;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        padding-top: 1rem;
        font-size: 0.9rem;
        opacity: 0.7;
    }

    .copyright :global(a) {
        color: #ffffff;
        text-decoration: none;
    }

    .copyright :global(a:hover) {
        color: var(--color-accent);
        text-decoration: underline;
    }
</style>
