# タスクリスト - 全セクションへの説明・著者表示の適用

- [x] `src/pages/[lang]/newsrelease` と `src/pages/[lang]/notes` の構造を確認する。
- [x] `src/content/config.ts` を確認し、`newsrelease` と `notes` コレクションのスキーマに `description` と `author` が含まれているか確認・更新する。
- [x] `newsrelease` 用のテンプレート (`src/pages/[lang]/newsrelease/[slug].astro`) を修正し、`description` と `author` を表示する。
- [x] `notes` 用のテンプレート (`src/pages/[lang]/notes/[slug].astro`) を修正し、`description` と `author` を表示する。
- [x] 開発サーバーで変更を確認する。
- [x] 日本語のロードマップドキュメントを作成・更新する。
